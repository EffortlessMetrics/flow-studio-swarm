## 2024-05-24 - Missing ARIA Labels on Copy/Reload Buttons in Flow Studio
**Learning:** Found several icon-only or functionally specific buttons (like "Copy" or "Reload") without accessible names via `aria-label` attributes.
**Action:** Always ensure that utility buttons such as "Copy" and "Reload" have proper `aria-label`s to inform screen readers of their specific functions.