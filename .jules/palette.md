## 2026-03-14 - explicit focus visible rules
**Learning:** Custom interactive elements like `.filter-btn` and `.run-control-btn` may lack explicit `:focus-visible` rules despite being semantic `<button>`s.
**Action:** When auditing or creating custom UI elements, ensure explicit `:focus-visible` rules are added to maintain consistent keyboard accessibility.
