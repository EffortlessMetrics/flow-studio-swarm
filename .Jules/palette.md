## 2024-05-22 - Standardizing Icon Buttons
**Learning:** The application lacked a standard class for circular icon buttons, leading to inconsistent inline styles.
**Action:** Created `fs-icon-button` utility class in `flow-studio.base.css` to standardize circular icon buttons (like Help/Info triggers). Use this instead of inline `border-radius: 50%` styles.
