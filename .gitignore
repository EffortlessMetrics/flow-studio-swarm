# Active run artifacts (transient)
/swarm/runs/
/RUN_BASE/

# Repo pack directory (belongs to consuming repos, not the runtime repo)
# See swarm/packs/examples/repo-pack-template.yaml for template
/.swarm/

# Smoke test artifacts (receipt-backed verification)
# Narrow to flowstudio_smoke/ to leave room for other artifacts (schemas, UX manifests)
/artifacts/flowstudio_smoke/

# Ad-hoc flow directories (should use swarm/runs/<run-id>/)
/gate/

# Test & selftest artifacts (root level and anywhere they might land)
test_output.log
selftest_metrics.jsonl
selftest_degradations.log
# Belt-and-suspenders: catch selftest reports in wrong locations
swarm/tools/**/selftest_report.json

# Agent-generated summary files (except in curated examples)
# These should live in RUN_BASE, not scattered across the repo
**/impl_changes_summary.md
**/test_changes_summary.md
**/test_summary.md
**/doc_updates_summary.md
**/fuzz_changes_summary.md
**/gate_fix_summary.md
# Allow curated examples to keep their summaries
!swarm/examples/**/impl_changes_summary.md
!swarm/examples/**/test_changes_summary.md
!swarm/examples/**/test_summary.md
!swarm/examples/**/doc_updates_summary.md
!swarm/examples/**/fuzz_changes_summary.md
!swarm/examples/**/gate_fix_summary.md

# Run artifacts that may slip in
/.runs/
*.log
!swarm/examples/**/*.log

# Build artifacts
/target/
**/*.rs.bk
Cargo.lock
*.bak
*BUILD_CONTEXT_MANIFEST*
BUILD_SUMMARY.md

# Flow Studio UI
# Contract A: commit compiled JS for "clone → run" reliability
# TS sources are in src/, compiled JS is committed and drift-checked in CI
swarm/tools/flow_studio_ui/node_modules/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Test & Coverage
.pytest_cache/
.benchmarks/
.coverage
htmlcov/

# IDE
.vscode/settings.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Playwright MCP artifacts
.playwright-mcp/

nul
